{
    "build": {
      "installCommand": "npm install",
      "buildCommand": "npm run build"
    },
    "rewrites": [
      { "source": "/(.*)", "destination": "/" }
    ],
    "routes": [
      { "src": "/.*", "dest": "/index.html" }
    ]
  }